export const APP_NAME = 'Ptero Manager'

/**
 * Egg IDs that expose a web interface accessible via http://{address}/
 * Servers using these eggs show an "Open" button instead of address copy.
 */
export const WEB_ACCESSIBLE_EGGS: number[] = [15]
